function gerenciarGaleriaModal(config){
    this.modal = document.querySelector(config.modal);
    this.imgGaleria = document.querySelectorAll(config.imgGaleria);

    function atribuirImg(){
        alert('ola');
    }
}